[package]
name = "aether-drivers"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "VirtIO-Accel protocol + SoftNPU reference backend"
publish = false

[dependencies]
aether-core = { path = "../core" }
aether-hal = { path = "../hal" }

[lints]
workspace = true
